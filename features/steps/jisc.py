import json
import os
import pprint
import re

from behave import when, then, given


@given('research data sets have been copied to the transfer location for'
       ' preservation')
def step_impl(context):
    """Step intentionally left empty for now."""
    pass

